{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ -d flutter ]; then cd flutter && git pull && cd .. ; else git clone https://github.com/flutter/flutter.git -b stable ; fi && flutter/bin/flutter config --enable-web && flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo 'sem dependências npm'",
  "framework": null,
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
